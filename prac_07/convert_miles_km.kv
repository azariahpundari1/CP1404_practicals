BoxLayout:
    orientation: 'vertical'
    BoxLayout:
        orientation: 'horizontal'
        TextInput:
            id: input
            text: '34'
            font_size: 48
            on_text: app.convert_miles()
            # had to look in salutions for this
        BoxLayout:
            orientation: 'vertical'
            size_hint: 1, 1
            Button:
                text: 'UP'
                on_press: app.handle_increment(1)
            Button:
                text: 'DOWN'
                on_press: app.handle_increment(-1)
#    Button:
#        text: 'Convert M to KM'
#        on_press: app.convert_miles()
    Label:
        id: output
        text: '54.717'
        font_size: 48
        color: (1, 1, 0, 1)